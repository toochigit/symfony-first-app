{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(userForm) }}

        {{ form_errors(userForm) }}

        {{ form_rest(userForm) }}

    {{ form_end(userForm) }}

{% endblock %}

{% block title %}

{% endblock %}