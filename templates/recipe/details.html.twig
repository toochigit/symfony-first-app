{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('recipe_index') }}">Retour</a>

    <h1>{{ recipe.title }}</h1>
    <h3> {{ recipe.kind }}</h3>

    <h3>Ingrédients</h3>
    <ul>
        {% for item in recipe.ingredients %}
            <li>{{ item }}</li>
        {% endfor %}
    </ul>
{% endblock %}

